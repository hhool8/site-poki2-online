{
  "name": "poki2-portal",
  "version": "1.0.0",
  "private": true,
  "description": "poki2.online — Free Browser Games Aggregation Portal",
  "scripts": {
    "gen-og": "node scripts/gen-og-image.js",
    "gen-favicons": "node scripts/gen-favicons.js",
    "build": "node scripts/gen-favicons.js && node scripts/gen-og-image.js && node scripts/build.js",
    "clean": "rm -rf dist",
    "deploy": "wrangler pages deploy dist --project-name poki2-portal"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "to-ico": "^1.1.5",
    "wrangler": "^3"
  },
  "engines": {
    "node": ">=18"
  }
}
